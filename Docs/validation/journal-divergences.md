# Journal des divergences & anomalies

> **But** : consigner tout écart constaté pendant la campagne (contrat, valeurs, couverture, perte de données), avec sévérité et action.
> **Alimenté par** : Axes 1–6. **Source de vérité** : le code (`TrialResponseRow`).

## Échelle de sévérité
- 🔴 **Bloquant** : donnée fausse, manquante ou perdue silencieusement → l'analyse chercheur est compromise.
- 🟠 **Majeur** : donnée exploitable mais ambiguë / mal nommée / partielle → risque d'erreur d'analyse.
- 🟡 **Mineur** : cosmétique / documentaire → à corriger sans urgence.

## Registre

> **Première alimentation du registre : 2026-07-28**, par confrontation directe au code (commit `de1f72ee`, aucune modification de code). Ces six entrées ne sortent pas de la campagne (non démarrée, gates G0–G5 encore ⬜) mais de la revue de complétude — elles sont **vérifiées**, pas hypothétiques. Détail complet : `Docs/project-state/revue-completude-2026-07-28.md`.

| # | Date | Axe | Champ / zone | Type (manquant / renommé / type / valeur / perte / couverture) | Constat | Attendu (code) | Sévérité | Action / chantier | Statut |
| :- | :-- | :-- | :-- | :-- | :-- | :-- | :-: | :-- | :-- |
| D-001 | 2026-07-28 | Hors campagne (revue) | `acceptability_question` → ligne entière | perte | **Toute trial en condition `advisor = none` est jetée.** `TrialQuestionsUI.cs:91` ne pose pas la question sans advisor → `FlowSerializationUtility.cs:45` laisse le champ `null` → `TrialManager.cs:119-124` annule l'envoi avec un simple `Debug.LogWarning`. La condition contrôle ne remonte jamais en base. | La ligne doit partir avec le gameplay complet, colonnes questionnaire à `null` (mécanisme `[IncludeNullInJson]` déjà présent) | 🔴 | **Lot A1** + arbitrage Q-DATA-1 (valeur attendue pour un trial sans advisor) | Ouvert — non corrigé |
| D-002 | 2026-07-28 | Hors campagne (revue) | Modèle de pénalité (doc) | valeur | `TDD.md` décrit une pénalité de **-2 bugs** par nuage sur **18 occurrences** (`:238`, `:1207-1209`, `:1280-1282`, `:1364-1366`, `:1388`, journal `:1401`). Depuis la MàJ du 28/07, contredit frontalement `avancement.md`. | **-1 bug vert** par nuage, jamais de rouge, jamais sous zéro (`GameManager.cs:262-275`, `BugCloud.AddBugs`) — conforme GDD | 🟠 (doc, mais règle de scoring centrale) | Correction `TDD.md` appliquée le 28/07/26 (protocole `!doc`, diff validé, journal §4.2.7) | **Doc corrigée** — reste le sign-off chercheur Q-007 |
| D-003 | 2026-07-28 | Hors campagne (revue) | `acceptability_question`, `sens_of_agency_question`, `human_likeness_question` | renommé | Le **schéma SQL de référence** (`specs/multi-screen-flow/spec-tech.md:758`), la spec (`:448`) et `TDD.md:2476,2528,2614` déclarent `q1_response`/`q2_response`/`q3_response`. Si la table Supabase suit ce schéma, les 3 réponses n'atterrissent nulle part. | Les 3 noms émis par `FlowDataModels.cs:538-540` | 🔴 si confirmé | **Vérifier le schéma réel de la table Supabase** — le code ne l'expose pas. Puis aligner doc ou table. Bloque G4/G5 | Ouvert — hypothèse à lever |
| D-004 | 2026-07-28 | Hors campagne (revue) | `advisor_forced` ↔ `meta_choice_is_forced` | — (faux positif) | **Requalification de PC-1** : aucune dérive. `advisor_forced` est un champ de `BlockConfig` (config **entrante**, `FlowDataModels.cs:99`), `meta_choice_is_forced` un champ de `TrialResponseRow` (donnée **sortante**, `:430`). Deux couches distinctes, les deux noms sont corrects. | — | 🟢 | PC-1 réduit à son seul volet questionnaire (cf. D-003) et rétrogradé 🔴 → 🟠 | **Clos** |
| D-005 | 2026-07-28 | Hors campagne (revue) | `build_version` | valeur | Deux défauts coexistent (`FlowController.cs:26` = `0.4.0`, `SessionManager.cs:26` = `1.0.0`). Tranché : `SessionManager.cs:115` écrase avec `flow.BuildVersion` dans `CopyConfigFromFlowController()` → **en session pilotée par le flow, la colonne vaut toujours `0.4.0`**. | Une source unique de version | 🟡 | **Lot A5** — déterministe, donc pas un risque d'intégrité ; unifier pour la lisibilité | Ouvert — non corrigé |
| D-006 | 2026-07-28 | Hors campagne (revue) | `choice_correct`, `true_cloud` | valeur | **Départage à égalité non aléatoire.** `GameManager.cs:404` utilise `_leftCloud.greenBugs > _rightCloud.greenBugs` : à égalité, le nuage de **droite** est systématiquement déclaré correct. Atteignable en cours de trial — les pénalités s'arrêtent à zéro (`BugCloud.AddBugs`), donc deux nuages vidés sont à égalité. Avec les défauts (`total=20`, ratio 0.5 → 10 verts ; `trap_count=10`), un participant en difficulté y arrive : **le biais se concentre sur les trials les moins bien joués**. Le TDD décrivait à tort un `null` en cas d'égalité. ⚠️ Complément 28/07 (revue de couverture) : une **seconde convention, contradictoire**, existe dans `BugCloudGenerationUtility.cs:27` (`BestCloudIndex` désigne le **premier** nuage à égalité). | Comportement à définir par le chercheur (tirage / `null` / toujours correct / statu quo), **et unifié entre les deux emplacements** | 🟠 | **Q-TIE-1** ouverte le 28/07/26 ; correctif de quelques lignes si l'option D n'est pas retenue | Ouvert — arbitrage chercheur |

> **Deuxième alimentation : 2026-07-28**, par la **revue de couverture bidirectionnelle** (`Docs/project-state/revue-couverture-2026-07-28.md`, même commit `de1f72ee`, aucune modification de code). D-007 → D-015 proviennent de la confrontation du code (a) au **CSV de référence client** `Docs/references/CSV_BUGS_Output_V1.xlsx` — confrontation jamais faite jusqu'ici — et (b) à l'intégralité de `Docs/`. Toutes sont **vérifiées**, pas hypothétiques.

| # | Date | Axe | Champ / zone | Type (manquant / renommé / type / valeur / perte / couverture) | Constat | Attendu (code) | Sévérité | Action / chantier | Statut |
| :- | :-- | :-- | :-- | :-- | :-- | :-- | :-: | :-- | :-- |
| D-007 | 2026-07-28 | Hors campagne (revue) | `valley_advisor_choice_match`, `target_choice_match_advice`, `motor_choice_match_advice` | manquant | **Les 3 colonnes n'existent pas dans le code.** `specs/free-forced-choices/spec-fonc.md:216` (R13) et `:319` les déclarent « colonnes **existantes** » et spécifient leur calcul en mode forced ; elles viennent en réalité du CSV client (`AO4`, `BI4`, `BP4`) et n'ont jamais été implémentées. **L'advice-taking — variable dépendante comportementale n°1 (H1, H4, H6) — n'a aucune colonne dédiée.** Recalculable au distal (`valley_choice` vs `distal_advice_choice`) et sur le chemin (`followed_advisor_path`) ; **pas au niveau moteur** (cf. D-008). | Décision : implémenter les 3 colonnes, ou acter par écrit qu'elles sont recalculables post-hoc et corriger R13 | 🔴 | **Lot E3** + **Q-MOTOR-1** | Ouvert — arbitrage chercheur |
| D-008 | 2026-07-28 | Hors campagne (revue) | `motor_*` (résultat) | manquant | **Le motor advice n'émet aucune donnée de résultat.** `TrialResponseRow` ne porte que `motor_forced_*` et les deux **probabilités** `motor_advice_visible/reliable_probability`. Ni `ActiveSet`, ni `DisplayedSet`, ni `AdviceVisible`, ni `AdviceReliable` (tous présents dans `MotorAdviceController`) ne sont loggés. **Le niveau moteur — un des 3 niveaux d'intention qui portent les hypothèses — est inanalysable.** | 4 colonnes réalisées : set actif, set affiché, advice affiché, advice fiable | 🔴 | **Lot E1** ; requalifie **Q-002** de « traçabilité » 🟠 en **arbitrage bloquant** 🔴 | Ouvert — arbitrage chercheur |
| D-009 | 2026-07-28 | Hors campagne (revue) | `left/right_valley_*_bugs_nb`, `valley_total_bugs_nb` | manquant | **Le stimulus distal réellement affiché n'est pas enregistré.** `DistalChoiceUI.LeftScanData` / `RightScanData` (`:36-37,67-72`) portent les valeurs vues par le participant et **ne sont lues nulle part ailleurs**. La ligne ne contient que `distal_scene` (les **bornes** de config) et `distal_best_valley`. Asymétrie : côté proximal les valeurs réalisées **sont** loggées via `map_config`. | Les valeurs réalisées par vallée (total, verts, rouges), comme pour les nuages proximaux | 🔴 | **Lot E2** + **Q-DISTAL-1** | Ouvert — arbitrage chercheur |
| D-010 | 2026-07-28 | Hors campagne (revue) | QuestionnaireScene / `QuestionConfig` | couverture | **Requalification de E1 : la scène n'est jamais chargée.** `grep "AdvanceToPhase("` → 12 sites, **aucun** vers `GamePhase.Questionnaire` ; la scène est activée dans le build (index 8) mais inatteignable. Blocage **triple** : (1) `QuestionConfig` est un **type orphelin**, référencé par aucun champ de `SessionConfig`/`BlockConfig` — le chercheur ne peut rien saisir ; (2) liste hardcodée vide (`QuestionnaireUI.cs:38`) ; (3) `ApiClient.QueueQuestionnairePatchForTrial:136-147` n'extrait **que** `human_likeness_question` et jette les autres réponses. ⇒ **Trust in Technology Questionnaire** et questions block-wise sans chemin de données ni en entrée ni en sortie. | Décision de scope, puis extension du modèle (`BlockConfig.questions`) **et** du contrat de PATCH | 🔴 | **Q-QUEST-1 à reformuler** (ce n'est pas un branchement) + **Q-TRUST-1** | Ouvert — arbitrage chercheur |
| D-011 | 2026-07-28 | Hors campagne (revue) | `human_likeness_question` | valeur | **Répliqué sur TOUS les trials du bloc.** `ApiClient.QueueHumanLikenessPatchForBlock:150-186` boucle `for (trialIndex = 1; trialIndex <= trial_count; trialIndex++)`. Contredit **DEC-013** (« PATCH sur la **dernière** ligne du bloc »), `dictionnaire-donnees.md:110` et `matrice-tracabilite.md:130`. Un script qui moyenne la colonne sur les lignes surpondère la réponse ×`trial_count`. **Ferme PC-7** : le PATCH atterrit sur toutes les lignes, par construction. | Comportement peut-être correct (cohérent avec DEC-011, CSV sans jointure) mais **doit être écrit** | 🟠 | **Q-HL-1** ; corriger `dictionnaire-donnees.md` + DEC-013 selon l'arbitrage | Ouvert — doc à aligner |
| D-012 | 2026-07-28 | Hors campagne (revue) | Ordre des options d'advisor ; genre du badge humain | couverture | **Aléas non seedés sur des manipulations expérimentales.** (1) `AdvisorChoiceUI` mélange (Fisher-Yates, `UnityEngine.Random`) l'ordre des 3 options à chaque affichage : le **biais de position sur le méta-choix est incontrôlable post-hoc** (ordre ni seedé, ni loggé, ni spécifié). (2) Le genre du badge « advisor humain » est tiré à 50/50 au `Awake` **indépendamment dans 3 composants** (`AdviceExplanationUIBase`, `DistalChoiceUI`, `MotorAdviceUI`) → sur un même trial l'indicateur peut afficher une femme et l'encart d'explication un homme. Le reste du gameplay est correctement seedé via `LevelRegistry.CreateRng`. | Tirages seedés sur `trial_seed`/`block_seed`, ordre loggé, genre unifié par bloc | 🔴 | **Lot E5** + **Q-RANDOM-1** | Ouvert — arbitrage chercheur |
| D-013 | 2026-07-28 | Hors campagne (revue) | `display_probability` | manquant (doc) + valeur | **Zéro occurrence dans `Docs/`.** Tirage de Bernoulli **par trial** décidant si l'explanation proximale/motrice s'affiche (`FlowDataModels.cs:286`, `FlowController.cs:660-695`, ajouté le 17/07, commit `2b58981b`). Il **contredit TR7** de `specs/explanations-short-long/spec-fonc.md:196` (« granularité blockwise, tous les trials d'un bloc partagent la même configuration »). **Asymétrie non documentée** : le distal n'a pas ce tirage. Absent du dictionnaire, de la matrice et de `config-session-test.md` → un chercheur peut modifier la manipulation de **H8** sans qu'aucun document ne l'en informe. | Paramètre documenté + TR7 mise à jour + ajout à la matrice de couverture | 🔴 | Ajouter à `config-session-test.md` et au dictionnaire ; corriger TR7 dans la spec-fonc explanations | Ouvert — doc à produire |
| D-014 | 2026-07-28 | Hors campagne (revue) | `block_template_id` | manquant (doc) | Champ émis par le code (`FlowDataModels.cs:465`, ajouté par **DEC-022**) mais **absent du dictionnaire ET de la matrice**, tous deux mis à jour le 28/07. La matrice annonce « Total champs code = 80 » ; le décompte réel est **82**. | Colonne documentée, décompte corrigé | 🟠 | Corriger `dictionnaire-donnees.md` et `matrice-tracabilite.md` | Ouvert — doc à produire |
| D-015 | 2026-07-28 | Hors campagne (revue) | Table `participant_notes` | couverture (doc) | **3ᵉ endpoint non documenté** : `POST api/participant-notes` (`ApiClient.cs:41,233-246`), alimenté par `ParticipantNoteUI` (EndSessionScene). `grep -rn "participant-notes" Docs/` → aucun résultat. C'est pourtant la réponse au `final_comments` du GDD : **la doc sous-estime la couverture et le codebook est incomplet** — un chercheur ignore qu'il dispose de ces données. | Section « données hors `trial_responses` » dans le codebook | 🟠 | Ajouter au `dictionnaire-donnees.md` | Ouvert — doc à produire |

## Points chauds pré-identifiés (à confirmer / infirmer pendant la campagne)

| Réf | Zone | Hypothèse à tester | Sévérité potentielle |
| :-- | :-- | :-- | :-: |
| PC-1 | Noms de colonnes | ~~Dérive code (`advisor_forced`…) ↔ doc (`meta_choice_is_forced`…)~~ → **partiellement infirmé le 28/07/26 (D-004)** : les deux noms sont corrects, à des couches différentes. **Réduit à son volet questionnaire** : `q1_response` ↔ `acceptability_question` (D-003), à vérifier contre le schéma Supabase réel. | 🟠 (était 🔴) |
| PC-2 | Résilience réseau | `ProcessPendingTrialRequests` `break` au 1er échec ⇒ file entière bloquée (`ApiClient.cs:349-353`) — **confirmé le 28/07/26** (revue §3, D3), non corrigé (Lot A3) | 🔴 |
| PC-3 | Persistance | File d'envoi en mémoire, non persistée ⇒ perte à la fermeture / crash — **confirmé le 28/07/26** (revue §3, D2), non corrigé (Lot A2) | 🔴 |
| PC-4 | Reproductibilité | `trial_seed` = `session.randomizationSeed` (potentiellement constant) au lieu du seed per-trial (`TrialManager.cs:269`) | 🟠 |
| PC-5 | Précédence config | `SessionManager` prime sur `FlowController.ActiveMapConfig` dans `BuildBaseRow` | 🟠 |
| PC-6 | Sérialisation | Champs null omis du JSON (hors `[IncludeNullInJson]`) ⇒ colonnes absentes sur certaines lignes | 🟠 |
| PC-7 | PATCH questionnaire | ~~`human_likeness_question` patché « par bloc » : atterrit-il sur la bonne ligne ?~~ → **CLOS le 28/07/26 (D-011)** : il atterrit sur **toutes** les lignes du bloc, par construction (`ApiClient.cs:150-186`). Reste l'arbitrage Q-HL-1 et l'alignement doc/DEC-013. | 🟠 → **clos**, reporté sur D-011 |
| PC-8 | Complétude données | Positions pièges & bugs verts nuage non-choisi non exportés | 🟡 |
| PC-9 | Sémantique | `followed_advisor_path` = chemin affiché, pas optimal | 🟡 (doc) |
